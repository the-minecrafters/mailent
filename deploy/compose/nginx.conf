server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    location /api/ { proxy_pass http://core:8080; }
    location = /ready { proxy_pass http://core:8080; }
    location = /health { proxy_pass http://core:8080; }
    location / { try_files $uri $uri/ /index.html; }
}
