# Continue Mailent — finish and verify, preserve existing work

User instruction: finish the real product against `notes/problem_statement.md` (gitignored; read explicitly), not merely cosmetic changes. Work autonomously. Preserve approved homepage visual design, improve concrete copy. Do not claim completion without runtime evidence. Use free infrastructure only. No further permissions needed for routine fixes, GitHub release, or existing Render deployment. Never expose credentials.

## Resume state — 2026-09-24, usage handoff at 2% remaining
Repo `/home/dipak/code/mailent`, branch main, HEAD `98ad762a6d4a9b4a0a008c2995aaf1cb15e2ca50`. Current changes UNCOMMITTED. Read git status/diff before editing. `VERIFICATION_REVIEW.md` records the preceding audit; its first two failures are now fixed locally, and connection evidence rendering has been replaced. Do not discard either file or tests.

Implemented this turn:
- Assessment JSON/HTML/PDF export checks the caller's workspace before loading evidence (`apps/core/src/api/reports.rs`). Missing DNSSEC status now remains unknown.
- Added `JobRepository::cancel_active_for_device` in repository, in-memory, PostgreSQL and DualStorage. Revocation cancels all pending/leased/running device jobs scoped to organization, with a single database UPDATE, rather than capped list of 200. Preserves unrelated and terminal jobs.
- Replaced fabricated ProtocolLadder with evidence-only connection stages. No automatic TCP success, no ClientHello/version implying TLS completion, no unknown-as-plaintext, no end-to-end claim. Certificate validity is evaluated at capture time. Flexible readable cards.
- Four frontend regression tests in `apps/web/src/components/ProtocolLadder.test.tsx`.
- Homepage copy specifies PCAP/SMTP/IMAP/POP3, Zeek, TLS/cipher/Forward Secrecy, monitoring, reports, and fixes; primary CTA is capture analysis. Visual layout preserved.
- Assessment risk panel removes Jev/internal engine/model branding; retains AI-assisted versus policy-based provenance and risk review. Privacy must retain vendor disclosures.

Validation:
- Two original backend regressions passed after fixes: `/tmp/mailent-fixed-regressions.log`.
- Tests then strengthened in `apps/core/tests/verification_review.rs`: cross-org exports across all 3 formats, 201+ jobs plus leased/running, unrelated-org/device/terminal preservation. Full `cargo test -p mailent-core` launched and STILL compiling at last check, session 87171, output `/tmp/mailent-fixed-core-tests.log`. Inspect outcome; do not claim passed until done. If session unavailable inspect process/log then rerun as needed.
- Frontend 18 tests / 4 files passed: `/tmp/mailent-fixed-web-tests.log` (test file renamed .tsx because config only discovers .test.tsx).
- `pnpm --dir apps/web build` passed: `/tmp/mailent-fixed-web-build.log`, existing large bundle warning.
- `git diff --check` passed.
- Local homepage opened successfully in browser at http://127.0.0.1:5173; browser accessibility tree confirms new copy, primary capture CTA and intact sections. No screenshot/desktop overflow acceptance yet. Vite running via session2912 (check actual port if occupied). In-app browser tab2 local homepage.
- No commit, push, new release, or deployment this turn.

## Mandatory remaining work (prioritize actual correctness)
1. Read entire `notes/problem_statement.md`. Minimum: passive PCAP, SMTP/IMAP/POP3 session reconstruction, STARTTLS/STLS, actual TLS/cipher/key-exchange/Forward Secrecy, X.509 extraction + CHAIN VALIDATION + validity/algorithms/key lengths, deterministic weak crypto findings, AI risk/anomaly/prioritization and mitigation, comprehensive JSON/PDF/HTML reports + dashboard. Zeek8+ REQUIRED. Ongoing live mail-server monitoring is core. Active domain checks are complementary.
2. Passive chain validation absent: `apps/sensor/src/normalize.rs:432` hardcodes ChainValidation::NotVerified. Inspected official installed Zeek8.0.4 script in container: `/usr/local/zeek/share/zeek/policy/protocols/ssl/validate-certs.zeek` performs x509_verify(chain, root_certs) in SSL::ssl_finishing; exports logged `validation_status` string; `validation_code` exists but is NOT logged by default. SSL base __load__ already loads mozilla-ca-list. Current `zeek/mailent/__load__.zeek` DOES NOT load validate-certs. Both offline and live embed this script. Implement proper validation, preserving unknown when certs unavailable (TLS1.3). Verify capture-time semantics/root trust, missing intermediates; do not map arbitrary missing status to failure. Tests trusted/untrusted/expired/incomplete chains and actual PCAP run required. No edits made here yet.
3. Unify deterministic score and baseline risk across CLI/web/reports. CLI uses correlation::compute_posture and grade-based risk; web manual100-minus25/15/8/3 and max finding/provider risk. Same smtp_starttls.pcapng currently CLI60/Moderate/MEDIUM versus web85/B/HIGH despite one HIGH expired-cert finding. High finding is real: CNmail.mailent.test expired2021-01-01, captured2026-09-21. Keep AI assistance separate from deterministic facts, no lowering known high risk from category averaging. CLI dedup rule+title can discard per-session evidence: inspect.
4. AI evidence: capture pipeline passes anomalies_count while Jev fallback expects anomalies array; provide minimized real feature evidence and preserve real provider response/provenance. Do not fabricate AI or replace it with hardcoded attack scenarios. Review speculative hardcoded threat matrix/remediation added in 8a64c38.
5. Windows installer false readiness: `scripts/install.ps1` and `apps/web/public/install.ps1` mutate files/PATH before dependency check, ignore native exit codes on exe --version/container pull, print success without required Zeek. Verify dependencies and native exit codes before committing installation; no misleading readiness. `scripts/mailent-zeek.cmd` always --network none, unsuitable live host capture. Reject unsupported Windows container live capture clearly; use native Linux/WSL collector pathway with real capture permissions. Keep site/release copies identical; test failures. No edits made here yet.
6. Revocation integration: pending jobs now fixed but prove CLI token rejection, daemon stopping while busy, live capture shutdown and credential clearing. Existing monitor listener error may return before credentials cleared. Guests should continue seeing sign-in without approval form/403, preserve device code across auth.
7. Finish copy sweep. Homepage interactive `approaches` at file start still says 'See what really happened', 'Keep up with changes'; default domain tab despite primary PCAP CTA. Bottom CTA also vague. Retain layout, make accurate and specific. Check 'Linux / macOS' button against actual Linux x86_64 release availability. Workspace privacy/vendor details belong on Privacy page, not constant banners. Do not remove real analysis capabilities.
8. Check actual rendered desktop layout, connection card wrapping, long device names/status/version, download controls and all report formats. Current browser check is accessibility-tree only, not pixel screenshot. Use CUA tools.
9. Review report isolation broadly, not just endpoint. Current scoped assessment gate precedes EvidenceSnapshot::assessment; check synced references cannot point at another org's sessions/assets. Existing tests use in-memory; PostgreSQL bulk UPDATE compiles but not DB runtime verified yet. Load Supabase/Postgres skills before DB tasks. Do not access unrelated project data.
10. `SHOWCASE.md` contains a tracked literal account password. Do not echo it in output. Remove plaintext documentation credential and flag rotation if still active; never embed credentials in handoff/commits.

## Existing infrastructure/release
- GitHub https://github.com/the-minecrafters/mailent, gh authenticated. Latest releasev0.1.2/98ad762, workflow35992247290 succeeded. AssetsLinux+Windows archives/checksums/install scripts. User authorized release.
- Render service srv-dapu2q1srm7s73arpte0, https://mailent.onrender.com, free, autoDeploy off. Last verified live commit460db8db5f4d769abb89d8fe2137b25c27ae781e, deploydep-daqeei8u01pc73fsb38g. Production is behind current source. Live /install.ps1 returned SPA HTML. Must ship static script correctly.
- After complete tests, scoped commit/push, release bump (likely0.1.3), tag/workflow/assets verification, deploy existing Render service pinned to commit, verify actual live SHA/install scripts/auth/report endpoints. Do not deploy unverified security regressions.
- Supabase project vuskxbttfmmyxpqudjwv, orgyomqbuehrsgytvkrmjwp. Preserve existing data/private mailent schema. No new paid resources. Verify existing grade-width migration if needed; do not recreate project.
- SMTP free workaround: authenticated local device runs allowed server checks and submits viaHTTPS; cloud port25 blocks must remain honest inconclusive, never invented handshake success.

## Useful prior validation
Prior baseline97Rustpassed3ignored (some DB tests skip if unconfigured),14frontendpassed; logs `/tmp/mailent-verify-rust-full.log`, `/tmp/mailent-verify-web-tests.log`. Actual Zeek PCAP: `target/debug/mailent analyze fixtures/pcap/smtp_starttls.pcapng --zeek /home/dipak/code/mailent/scripts/mailent-zeek --format json --output-dir /tmp/mailent-current-review-reports`; prior output `/tmp/mailent-current-review-pcap.json`, reports in outputdir. SHA c2877835223b21abe61a522d9a6c96741707ca2d0d28d1eb0425d453e4ba06e6. Use existing Zeek Podman8.0.4. Run relevant broader Rust checks with MAILENT_ZEEK configured if needed. Do not treat ignored/skipped tests as runtime acceptance.

User asked to leave this handoff when remaining usage reached2%; threshold reached. Continue from the dirty tree, finish requirements, validate, release/deploy; do not restart or replace the project. Report clearly what is local versus live and what remains unverified.
